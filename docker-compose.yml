services:

  ### PHP ###################################################
  php:
    build:
      context: ./
    container_name: siriusphp_invokator
    volumes:
      - ./:/var/www/html:delegated
    ports:
      - "9099:9000"

#Volumes
volumes:
  local:
    driver: local
